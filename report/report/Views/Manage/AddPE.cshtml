@model  List<report.Models.PromExport>

    @{

        foreach (var item in Model)
        {
        }

        
            @Ajax.ActionLink(Model.StatusName, "Edid_state_s", new { id = Model.Id }, new AjaxOptions { UpdateTargetId = "Status_" + Model.Id }, new { @style = "color: #00ff00; margin-right: 10px;", @name = Model.StatusName })
        

    }
    @section Scripts {
        @Scripts.Render("~/scripts/jquery-1.10.2.min.js")
        @Scripts.Render("~/scripts/jquery.unobtrusive-ajax.min.js")
    }
